import ItalianCar from "./Car"
// all funcitions will be static
export default class CarPhysics {
    //Things to implement:

    //Input Processing
    /**
     * @function InputProcessing  
     * @description Process user input events to determine the current state of arrow key presses.
     * @param events - array of input events since last frame.
     * @return array of booleans representing which arrow keys are pressed.
     */
    //acceleratoin/Velocity update ( we can use some sort of sqrt function to make it approach max speed asymptotically)

    //Turing/Steerign

    //Friction/Drag ( @Thomas)

    // Collision Detection/Response 

}